language: objective-c
cache: cocoapods
podfile: Example/Podfile
before_install:
- gem install cocoapods # Since Travis is not always on latest version
osx_image: xcode7.1
xcode_sdk: iphonesimulator9.1
xcode_workspace: Example/OCSlimProject.xcworkspace # path to your xcodeproj folder
xcode_scheme: BuildTestAllTargets-iOS
#script:
#- set -o pipefail && xcodebuild test -workspace Example/OCSlimProject.xcworkspace -scheme BuildTestAllTargets-iOS -sdk iphonesimulator9.1 ONLY_ACTIVE_ARCH=NO | xcpretty
#- pod lib lint


## DEFAULT TEMPLATE REFERENCE 
#language: objective-c
# cache: cocoapods
# podfile: Example/Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example
#script:
#- set -o pipefail && xcodebuild test -workspace Example/TravisTest.xcworkspace -scheme TravisTest-Example -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty
#- pod lib lint


